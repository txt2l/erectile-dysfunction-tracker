[phases.setup]
nixPkgs = ['nodejs_22', 'openssl']

[phases.install]
cmds = ['npm install --legacy-peer-deps'] # Force npm with legacy-peer-deps

[phases.build]
cmds = ['npm run build']

[phases.start]
cmd = 'node dist/server.js'
