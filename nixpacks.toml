[phases.setup]
nixPkgs = ['nodejs_22', 'openssl']

[phases.install]
cmds = ['npm install'] # Force npm, ignore pnpm-lock

[phases.build]
cmds = ['npm run build']

[phases.start]
cmd = 'node dist/server.js'
